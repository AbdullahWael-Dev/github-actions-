name: Basics Custom Actions
run-name: ${{github.actor}} explain-custom-actions
  
on:  
  workflow_dispatch

jobs:
  hello-world: 
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello World
        uses: ./.github/actions/print-hello-world
